# product-01 Heartbeat Report
- timestamp: 2026-05-13T22:07Z
- status_before: in_progress
- status_after: completed (서버사이드 vision API 연결, 실 API 호출 검증은 사용자 키 필요)

## actions_taken
- workflow-master 분류: 단일 기능 (mock judge → 서버사이드 Claude Vision API) · 중간 복잡도 · 기존 prouct-01 산출물 범위 내 → 단일 실행자로 진행 가능 (4-role 관점은 메모로만 정리: planner = API 계약, developer = zod+SDK 폴백, marketer = `ai-scoring-prompt.md` 톤 보존, operator = 환경 변수 + 안전 폴백 경로).
- 의존성 추가: `zod@4.4.3` · `@anthropic-ai/sdk@0.96.0` (pnpm add → 8 packages).
- 신규 파일:
  - `src/lib/score-schema.ts` — `ScoreRequestSchema` (image base64 ≤ 7.5MB, mime jpeg/png/gif/webp, memo ≤ 500자, toneMode soft/casual) + `ScoreResultSchema` (score 0-10, comment ≤ 120자, tags ≤ 2).
  - `src/lib/score-prompt.ts` — `ai-scoring-prompt.md` 원문 시스템 프롬프트, `{{toneMode}}` 치환.
  - `src/lib/score-client.ts` — `judgeWithFallback({file, memo, tone})` 클라이언트 래퍼. 사진 없음 / 미허용 mime / fetch 실패 / 비-200 → `mockJudge` 폴백. 성공 시 `source: "ai"` + model 표기.
  - `src/app/api/score/route.ts` — Node runtime POST 핸들러. `ANTHROPIC_API_KEY` 미설정 → 503 `scoring_disabled`. SDK 응답 텍스트에서 JSON 추출 → zod 재검증 → 200 또는 502.
  - `.env.example` — `ANTHROPIC_API_KEY` (필수), `SCORING_MODEL` (기본 `claude-sonnet-4-6`).
- 변경 파일:
  - `src/app/add/page.tsx` — `mockJudge` 직접 호출 제거. `File` state 추가. 결과 배지가 `result.source`에 따라 "AI"(positive 색조) / "mock"(muted) 동적 표시. 하단 안내 한 줄은 "키 있으면 AI, 없으면 mock"으로 갱신.
  - `README.md` — 스택/데이터 섹션에 AI 채점 흐름, `/api/score` 요청·응답 스펙, 상태 코드 표, 환경 변수 표 추가. 다음 단계 목록 갱신.
  - `infinity/artifacts/product-01/implementation.md` — `## Vision API wave (2026-05-13)` 섹션 추가, 한계와 Next Actions 갱신, 1번 완료 마킹.

## verification
- `pnpm typecheck` ✅ PASS (`@anthropic-ai/sdk`의 `ContentBlock` 타입 호환을 위해 type-predicate 대신 inline 분기로 텍스트 합성)
- `pnpm lint` ✅ PASS (warning 0)
- `pnpm build` ✅ PASS — 8 routes prerendered. `/api/score` = `ƒ (Dynamic)`, 다른 페이지는 모두 `○ (Static)`. First Load JS 105–139kB (Anthropic SDK는 서버 번들에만 포함).
- 실 API 호출 검증: 사용자가 `.env.local`에 키를 설정하지 않은 상태이며 실호출은 forbidden 범위에 가까워 보류. 코드 경로/스키마/폴백은 빌드와 타입 레벨에서 검증.

## local_commit
- `b015ec8` — 서버사이드 Claude Vision 채점 API 추가 (mock 폴백 보존)

## changed_files
- `package.json`, `pnpm-lock.yaml`
- `.env.example` (신규)
- `README.md`
- `src/app/add/page.tsx`
- `src/app/api/score/route.ts` (신규)
- `src/lib/score-client.ts` (신규)
- `src/lib/score-prompt.ts` (신규)
- `src/lib/score-schema.ts` (신규)
- `infinity/artifacts/product-01/implementation.md` (인텔리전스 메모 갱신 — 다른 레포)

## blockers
- 실 Anthropic API 호출 검증을 수행하지 않음. 검증하려면 사용자가 `.env.local`에 유효 키 + 모델 ID를 넣고 `pnpm dev`로 사진 1장 업로드 → 결과 카드의 "AI" 배지 확인 필요.
- `SCORING_MODEL` 기본값 `claude-sonnet-4-6`이 현재 사용자 계정에서 가용한지 확인 못 함. 미가용 시 `SCORING_MODEL`을 가용 모델로 덮어쓰면 됨 (예: `claude-sonnet-4-5`).
- Next.js 15.1.6 보안 권고(CVE-2025-66478)가 pnpm install 시 경고 — 이번 wave 범위 밖.

## next_actions
1. 사용자가 키 설정 후 dev 서버에서 실제 사진으로 채점 한 번 검증 (배지가 "AI"로 표시되고 톤이 가이드대로 나오는지).
2. AI 실패 → mock 폴백 시 사용자에게 보일 토스트 한 줄 카피 결정 (현재는 배지로만 구분, marketer 관점 추가 작업).
3. 서버사이드 일일 30덕 캡 검증 추가 — 현재 클라이언트 가드만 있어 API 단독 호출 시 우회 가능.
4. 동일 사진 중복 업로드 감지 (perceptual hash) — design.md §5 악용 방지 항목.
5. 사진 영속화 결정 (Supabase Storage / R2 / 로컬 파일) → store에 photoUrl 영구 URL 저장.
6. Next.js 15.1.6 → 15.x 패치 버전 업그레이드 검토 (CVE-2025-66478).
