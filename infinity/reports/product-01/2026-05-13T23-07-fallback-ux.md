# product-01 Heartbeat Report
- timestamp: 2026-05-13T23:07Z
- status_before: in_progress
- status_after: in_progress

## actions_taken
- workflow-master 분류: 단일 UX 보정 (배지 외에 폴백 안내 1줄 추가) · trivial · 기존 product-01 산출물 범위 내 → 단일 실행자 진행. 4-role 관점은 코드/문서 흐름에 그대로 반영: planner = 폴백 시그널 계약(`fallbackReason`), developer = 두 분기에서만 reason 세팅·UI 한 곳에서 토스트, marketer = 기존 copy-spec 톤 유지("AI가 잠깐 졸고 있어요. 임시 판정으로 보여드릴게요."), operator = 의도된 mock(사진 없음/미허용 mime)에는 토스트 미발생 → CS 노이즈 최소화.
- `src/lib/score-client.ts`:
  - `IFallbackReason = "api_error" | "network_error"` 타입 신설.
  - `IJudgeOutcome`에 `fallbackReason?: IFallbackReason` 옵셔널 필드 추가.
  - `res.ok === false` 분기 → `fallbackReason: "api_error"`로 mock 반환.
  - `catch` 분기 → `fallbackReason: "network_error"`로 mock 반환.
  - 사진 없음/미허용 mime는 의도된 mock이라 `fallbackReason` 미설정 (토스트 노출 안 됨).
- `src/app/add/page.tsx`:
  - `runJudge`에서 결과 수신 직후 `outcome.fallbackReason`이 있으면 `showToast("AI가 잠깐 졸고 있어요. 임시 판정으로 보여드릴게요.")` 1회 호출.
  - 결과 배지(`AI` vs `mock`) 동작은 기존 그대로.
  - 한 채점 사이클에서 토스트는 단 한 번 (실패→폴백 직후), 재채점 시도마다 새로 평가.
- `docs/copy-spec.md`: Operator 마이크로카피 표의 "AI 응답 실패 (가상)" 행을 두 줄로 분리.
  - `AI 응답 실패 → mock 폴백`: `AI가 잠깐 졸고 있어요. 임시 판정으로 보여드릴게요.` (이번 wave 적용본)
  - `AI 응답 실패 (재시도 유도)`: `잠깐 채점이 안 되네요. 다시 한 번.` (기존, 향후 retry UX용 보존)
- `infinity/artifacts/product-01/implementation.md`:
  - 알려진 한계의 "AI 실패 → mock 폴백 시 사용자에게 별도 안내 없음" 라인에 ✅ 해소 마크.
  - "Fallback UX wave (2026-05-13)" 섹션 추가 (클라이언트/페이지/카피/검증 4행 표).
  - Next Actions 2번에 키 없는 503 응답 시 폴백 토스트 1회 노출 확인 항목 추가.
- README 변경 없음 (기능이 1줄짜리 UX 보정, copy-spec.md/implementation.md로 충분 — 문서 부풀리기 회피).

## verification
- `pnpm typecheck` ✅ PASS
- `pnpm lint` ✅ PASS (warning 0)
- `pnpm build` ✅ PASS (8 routes, `/api/score`만 dynamic, First Load JS 105–139 kB, `/add` 25.2 kB → 139 kB 변화 없음)
- 실제 API 호출 / 브라우저 검증은 forbidden(외부 API 스모크 테스트 금지) 범위라 미수행. 토스트 발화는 코드 경로 + 타입 레벨에서만 확인.

## local_commit
- `ee7f8d0` — AI 채점 실패 시 폴백 안내 토스트 추가 (app repo `master`, push 없음)

## changed_files
- `src/lib/score-client.ts` (app repo)
- `src/app/add/page.tsx` (app repo)
- `docs/copy-spec.md` (app repo)
- `infinity/artifacts/product-01/implementation.md` (prompt-archive)
- `infinity/reports/product-01/2026-05-13T23-07-fallback-ux.md` (prompt-archive, 본 보고서)

## blockers
- 실제 사용자 경험 검증(브라우저에서 키 미설정 → 사진 업로드 → 토스트 1회 노출) 미수행 — allowed에 외부 API 스모크 테스트 금지가 있어 보류. 사용자가 dev 서버에서 `.env.local`에서 키 제거 후 `/add`에서 사진 채점 시 503 폴백 토스트가 정확히 1회 뜨는지 확인 필요.
- copy-spec.md 정리 시 기존 "재시도 유도" 카피(`잠깐 채점이 안 되네요. 다시 한 번.`)는 보존했지만 현재 UI에는 연결 지점 없음. 향후 명시적 retry UX 도입할 때 활용 가능.
- prompt-archive 사전 변경 파일(`THREADS_STYLE_HISTORY.md`, `infinity/EVALUATION_NOTES.md`)은 손대지 않음 — `git status`로 변경 없음 확인.

## next_actions
1. 사용자가 키 없는 상태에서 `/add` → 사진 채점 한 번 → 결과 카드 `mock` 배지 + 토스트 "AI가 잠깐 졸고 있어요. 임시 판정으로 보여드릴게요." 1회 노출 확인.
2. 재채점 버튼으로 연속 폴백 시 토스트가 사이클마다 새로 뜨는지 확인 (`showToast`는 모듈 싱글톤이고 자동 2.4s 디스미스라 중첩 없음).
3. AI 정상 응답 케이스에서는 토스트가 안 뜨는지 확인 (현재 코드상 `fallbackReason` 미설정 → 토스트 호출 분기 미진입).
4. 향후 retry UX 도입 시 copy-spec.md에 보존한 "재시도 유도" 카피와 retry 버튼/링크 연결.
5. 폴백 사유별 카피 분기(`api_error` vs `network_error`)는 현재 단일 메시지로 처리 — 사용자 피드백 보고 필요 시 분리.
