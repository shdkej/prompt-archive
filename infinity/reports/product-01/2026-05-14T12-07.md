# product-01 · 로컬 검증 리포트 (verify_local)

- timestamp: 2026-05-14T12-07 (Asia/Seoul 가정, 작성자 입력 기준)
- intent: `product-01` (`infinity/intents/active/product-01-virtue-rebirth-app.md`)
- artifact ref: `infinity/artifacts/product-01/implementation.md`
- app repo: `/home/ubuntu/dev/virtue-rebirth-app`
- mode: `verify_local` (Infinity L0/L1만 허용, L2/L3 금지)
- workflow-master: 앱 레포 내 `.agent/workflows/workflow-master.md` 및 `WORKFLOW-MASTER.md` **부재** → prompt-archive의 `WORKFLOW-MASTER.md` 가이드 적용, 작업을 좁게 유지(verify_local only).

## status_before

- 최근 커밋 5건 (newest first):
  - `70268fd` pin pnpm for deployment install
  - `2217c27` 배포 기본값을 mock 채점으로 고정
  - `ee7f8d0` AI 채점 실패 시 폴백 안내 토스트 추가
  - `b015ec8` 서버사이드 Claude Vision 채점 API 추가 (mock 폴백 보존)
  - `6ba58a8` MVP 디벨롭: 로컬 상태 / 판정 / 환생 진화 인터랙션 강화
- `git status --short` → 깨끗 (변경 없음).
- 환경 변수 (presence-only, 값 미노출):
  - `ANTHROPIC_API_KEY` = **absent**
  - `NEXT_PUBLIC_SCORING_MODE` = **absent**
- `.env.example`만 존재. `.env` / `.env.local` 부재 → 로컬에 실키 없음, 실호출 위험 없음.
- 코드 상태:
  - `src/app/api/score/route.ts` — `ANTHROPIC_API_KEY` 미설정 시 503 `scoring_disabled` 즉시 반환. 키 있어도 zod 파싱 실패 시 400, 모델 비-JSON/스키마 미일치 시 502.
  - `src/lib/score-schema.ts` — `imageBase64 ≤ 7.5MB`, mime 4종 화이트리스트, memo ≤ 500자, tone soft|casual. 결과 score 0-10 정수, comment ≤ 120자, tags ≤ 2.
  - `src/lib/score-client.ts` — `judgeWithFallback`는 파일/mime 없으면 mock, `NEXT_PUBLIC_SCORING_MODE !== "ai"`이면 mock(=배포 기본값 `2217c27`), 비-200 → `api_error`, catch → `network_error`. **즉, 클라이언트에서 실호출하려면 `NEXT_PUBLIC_SCORING_MODE=ai`도 같이 켜야 함.**
  - `src/lib/score-prompt.ts` — §5 톤 가이드 + 도덕적 칭찬/설교 금지 룰 포함, `{{toneMode}}` 치환 동작.

## actions_taken

1. workflow-master 파일 위치 확인 (앱 레포 내 부재 → prompt-archive 가이드로 폴백, 좁은 스코프 유지).
2. intent + implementation.md 재독으로 현재 next priority 확인.
3. score route / schema / client / prompt 4파일 정독, 폴백 분기 손상 여부 점검.
4. `git status` + `git log -5`로 로컬 상태 확인.
5. 환경 변수 **존재 여부만** 부울로 체크 (값 출력/저장/파일화 없음). `.env*` 파일 목록 확인.
6. L0 게이트 3종 실행: `pnpm typecheck`, `pnpm lint`, `pnpm build`.
7. L0 라이브 프로브: `pnpm start` (백그라운드) → `POST /api/score` 빈 페이로드, `GET /` 핑 → 종료.
8. 본 리포트 작성.

원본 코드 미수정. 비밀 미생성/미인쇄/미파일화. 푸시/배포/외부 메시지 없음.

## verification

| 검증 | 결과 | 비고 |
|------|------|------|
| `pnpm typecheck` | ✅ PASS | tsc --noEmit, 출력 없음 |
| `pnpm lint` | ✅ PASS | "No ESLint warnings or errors" |
| `pnpm build` | ✅ PASS | 8 routes, `/api/score`는 ƒ(Dynamic), First Load JS 105–139 kB |
| `pnpm start` boot | ✅ PASS | "Ready in 604ms" (Next 15.1.6) |
| `POST /api/score` (키 없음) | ✅ PASS · **HTTP 503** | body: `{"error":"scoring_disabled","message":"ANTHROPIC_API_KEY 미설정. 클라이언트는 mock으로 폴백."}` — 의도된 503 fallback 경로가 정상 동작 |
| `GET /` | ✅ PASS · HTTP 200 | dashboard 정적 prerender 응답 |
| 종료 후 포트 정리 | ✅ free | 8030/3997 모두 해제 |

추가 관찰:

- `package.json` `start` 스크립트가 `next start -p 8030`로 고정되어 있어, 검증용 임시 포트(3997)와 함께 `-p 8030 --port 3997`이 둘 다 listen 됨. 운영상 무해하지만 향후 port-override 시 혼선 가능 → 별도 dev/start 분리 또는 환경변수 기반으로 바꾸면 깔끔. (참고만, 변경 안 함.)
- 클라이언트 폴백 게이트가 두 단계임: (a) `NEXT_PUBLIC_SCORING_MODE === "ai"` 아니면 mock, (b) 그래도 503/exception이면 mock + `fallbackReason`. 실 키 E2E 시 둘 다 켜야 함 (`ANTHROPIC_API_KEY` 서버 측 + `NEXT_PUBLIC_SCORING_MODE=ai` 클라이언트 측).

## L2 assessment — 실 API E2E (다음 액션)

| 항목 | 평가 |
|------|------|
| impact | 중간. 정상 흐름은 로컬 단일 사용자 1회 호출 (사진 1장 + 메모). 외부 시스템 영구 변경 없음. |
| reversibility | 높음. 호출 자체는 멱등성 없으나 사용 후 영구 부작용 없음(서버사이드 DB 미사용). |
| cost | **있음**. Claude Sonnet 4.6 Vision 호출 비용 발생 (예: 1회 ~수 cent ~ 십 cent 추정). 무료 X. |
| production | 무관. 로컬 dev 한정. 운영 트래픽/배포 무영향. |
| secrets | **있음**. 실 `ANTHROPIC_API_KEY`를 `.env.local`에 두거나 셸에 export 해야 함. 키 노출/유출 위험 존재. |
| third-party messaging | 없음. Anthropic API 호출만, 사용자 외부 메시지/소셜 게시 없음. |

→ **cost + secrets 두 항목이 트리거되므로 자동 승인 불가. 사용자 명시 승인 필요.**

## blocked / next_actions

- ✅ 모든 L0 무비용 게이트 통과 (typecheck/lint/build + 503 fallback 라이브 프로브).
- 🚦 **blocked (pending user approval)**: 실 `ANTHROPIC_API_KEY` 기반 end-to-end Vision 호출 검증.
  - 필요 입력 1: 키 주입 방식 — (a) 사용자가 직접 `.env.local`에 적재 후 "키 세팅 완료" 신호, (b) 사용자가 1회 셸 export 후 에이전트가 같은 셸 컨텍스트 활용. 어느 쪽이든 에이전트는 키 값을 절대 읽지/인쇄하지/저장하지 않음 (presence-only 체크만).
  - 필요 입력 2: `NEXT_PUBLIC_SCORING_MODE=ai` 동시 활성화 동의 (클라이언트 게이트 해제, 비용 발생 명시 인지).
  - 필요 입력 3: 검증 범위 — 1회 호출만(최소) vs 톤 2종(soft/casual) × 1장씩 2회 vs 0점/저점/중점 3개 케이스. 기본은 **1회**.
  - 예상 비용 — 1회 호출 기준 입력 토큰 + 이미지 + 256 max_tokens. 대략 사용자가 인지하는 수준의 소액. (정확치는 Anthropic 단가 참조.)
- L0 후속 (승인 불필요):
  - `package.json` `start` 스크립트 포트 충돌 정리 (선택, 작은 DX 개선).
  - `.env.example` 주석에 `NEXT_PUBLIC_SCORING_MODE=ai` 필요 사실 명시 (현재 README/`.env.example` 둘 다 클라 측 플래그 강조 부족 가능 — 추후 별도 wave에서 점검).
- L2 후속 (다음 우선순위, 별도 의도/승인):
  - 영속화 결정 (Supabase vs SQLite + Tailscale).
  - 사진 영속화.
  - 서버 측 일일 30덕 캡 검증.
  - 배포 도메인 결정.

## user approval needed?

**YES, for the next meaningful gate (real Anthropic API E2E).**

- 사유: 비용 발생 + 실 API 키(secret) 취급. Infinity L2 룰상 자동 승인 불가.
- 에이전트가 받아야 할 명시적 동의 문구 예: "키 세팅 완료, 1회 실호출 진행해도 됨" (또는 케이스 수/톤 명시).
- 동의 전까지 본 리포트로 verify_local 단계 종료.
