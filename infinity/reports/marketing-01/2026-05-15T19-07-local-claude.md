# marketing-01 · 로컬 실행 결과 (Claude Code)

- timestamp: 2026-05-15T19:07Z
- agent: Claude Code (Opus 4.7, 1M context)
- repo: /home/ubuntu/dev/virtue-rebirth-app
- branch: master (origin/master 추적, push 완료)
- intent: marketing-01 (Virtue 활성화 감사 · demo-state + 텔레메트리)

---

## 1. workflow-master 점검

- `/home/ubuntu/dev/virtue-rebirth-app` 내 `.agent/workflows/workflow-master.md` 및 `WORKFLOW-MASTER.md` 모두 부재.
- intent의 안내(부재 시 기록 후 최소 계획)에 따라 다음과 같이 최소 계획으로 진행:
  1. 데모 가드 확인 → 2. error.tsx 신설 → 3. deed_judged/saved 프로퍼티 확장 → 4. typecheck/lint/build → 5. 보고서.
- 별도 workflow-master 부재로 인한 영향 없음. 추후 본 레포에 워크플로우 문서가 합류하면 동일 절차를 그쪽으로 흡수 가능.

## 2. 변경 파일

| 파일 | 변경 종류 | 요약 |
|------|-----------|------|
| `src/app/error.tsx` | 신규 | App Router 에러 바운더리. `posthog.captureException`으로 stack/message 노출. |
| `src/app/add/page.tsx` | 수정 | `deed_judged` 인텐트 이벤트 보강 + `deed_judge_completed` 결과 이벤트 신설 + `deed_saved` 프로퍼티 확장 + `runJudge` 내부 예외 명시 캡처. |

`git diff --stat` 결과:

```
 src/app/add/page.tsx | 35 ++++++++++++++++++++++++++++++++++-
 1 file changed, 34 insertions(+), 1 deletion(-)
```

신규 untracked: `src/app/error.tsx`.

### 2-1. 이벤트 스키마 변경 요약

- `deed_judged` (버튼 클릭, 인텐트):
  - 기존: `has_photo`, `has_memo`, `tone`
  - 추가: `memo_length`, `scoring_mode` (`ai` | `mock`). `has_memo`는 trim 기준으로 계산.
- `deed_judge_completed` (신규, 채점 직후):
  - `score`, `source` (`ai` | `mock`), `fallback_reason`, `model`, `has_photo`, `tone`, `memo_length`, `retry_count`, `duration_ms`.
  - 채점 호출(`judgeWithFallback`) 직후 호출되며, mock 폴백 여부도 함께 캡처.
- `deed_saved` (저장 성공):
  - 기존: `score`, `source`, `tags`, `level_up`, `new_species`
  - 추가: `fallback_reason`, `tag_count`, `has_photo`, `tone`, `memo_length`, `retry_count`, `total_after`.

### 2-2. 예외 캡처 보강

- `error.tsx`: PostHog SDK가 React 에러 바운더리에 잡힌 예외를 받지 못해 `$exception.message/type`이 null로 남던 문제를 직접 캡처로 해소.
- `runJudge` 내부 try/catch: `posthog.captureException`으로 명시 캡처 후 re-throw (UI 상태 정리 + 텔레메트리 양쪽 보장).
- `instrumentation-client.ts`: `capture_exceptions: true` 이미 활성 → 추가 변경 없음.

## 3. 데모 가드 검증

- `src/lib/store.ts:30` — `const SHOW_DEMO = process.env.NEXT_PUBLIC_SHOW_DEMO === "1";`
- `SEED_DEEDS`(line 31), `readSnapshot` 첫 로드 분기(line 84-89) 모두 `SHOW_DEMO`에 의해 통제됨.
- 기본값(미설정)은 빈 배열 → 신규 방문자 시드 데이터 노출 없음. `dfaaf4e` 커밋의 가드 정상 반영 확인.
- **주의**: 본 변경은 로컬 코드 한정. 프로덕션(Oracle, https://virtue.oracle.shdkej.com) HTML이 여전히 demo seed를 노출 중일 가능성은 별도 배포(pm2 restart + env `NEXT_PUBLIC_SHOW_DEMO=0` 명시)로만 해소 가능 — 본 실행에서는 의도적으로 차단됨(아래 §6 참조).

## 4. 검증 결과

| 항목 | 결과 |
|------|------|
| `pnpm typecheck` | ✅ PASS (tsc --noEmit, 0 error) |
| `pnpm lint` | ✅ PASS (0 error, 사전 존재 warning 4건 — 모두 sheet.tsx/toast.tsx/headline.tsx의 `react-hooks/set-state-in-effect`로 본 변경과 무관) |
| `pnpm build` | ✅ PASS (Next 16.2.6 Turbopack, 정적 페이지 7/7 생성, /add 정적 prerender) |
| 런타임 스모크 | ⏸ 미실행 (로컬 dev 서버 띄울 권한·필요성 없음 — 빌드로 충분) |

## 5. 커밋 / 푸시 상태

- approval: agent-approved L2
- 판단 근거:
  - marketing-01 목표와 직접 연결된 코드 변경이다.
  - 일반 git commit/push이며 force-push가 아니고 되돌림 가능하다.
  - 비용 발생, 시크릿/권한/프로덕션 데이터 변경, 타인 메시징이 없다.
  - 실행 전후 git 상태와 typecheck/lint/build 검증을 확인했다.
- commit: `a10ff0e` — `Improve PostHog activation telemetry`
- push: `origin/master` 완료 (`dfaaf4e..a10ff0e`)
- final git state: `master...origin/master` clean

## 6. 차단된 프로덕션 단계

다음은 본 실행에서 의도적으로 수행하지 않음 (intent의 Forbidden / 시스템 일반 가이드라인 준수):

- Oracle 서버 SSH 접속, `pm2 restart`, `pnpm build` (서버측), nginx 재로드.
- `ecosystem.config.js`에 `NEXT_PUBLIC_SHOW_DEMO=0` 추가 및 배포.
- 배포 후 ETag/HTML 신규 방문자 시뮬레이션 (`curl https://virtue.oracle.shdkej.com`).
- 위 작업은 사용자 승인 + 배포 절차(`infinity/artifacts/product-01/deployment-guide.md`) 기반으로 별도 수행 필요.

## 7. 7일 측정 가이드 (변경 후 기대)

- `deed_judge_completed.source` 분포로 AI/mock 비율 및 `fallback_reason` 빈도 파악.
- `deed_saved.retry_count` 분포로 사용자가 한 번 더 채점을 얼마나 시도하는지 확인.
- `deed_saved / deed_judged` 비율 (또는 `deed_saved / deed_judge_completed`)이 30% 이상 도달하는지 추적.
- `$exception` 이벤트의 `message/type` 필드가 더 이상 null 비율 100%가 아닌지 확인 (React 렌더 에러가 발생한 경우).

## 8. 결론

코드 레벨에서 marketing-01 텔레메트리 개선안의 핵심 3건(error.tsx, deed_judged 확장, deed_saved 확장)을 모두 적용했고 빌드/타입/린트 검증 통과. 데모 가드는 이미 정상 반영. 프로덕션 반영은 사용자 승인 하에 별도 배포 단계 필요.
