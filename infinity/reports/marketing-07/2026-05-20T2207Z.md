# marketing-07 Heartbeat Report

- timestamp: 2026-05-20T22:07Z
- intent: marketing-07 — Virtue 최소 생존 오디언스 기준표 작성
- mode: execute_local (local Claude Code)
- status_before: Inbox (구조화 대기)
- status_after: archived (success_criteria 충족)
- approval: agent-approved L2 (prompt-archive normal push; 아래 L2 체크리스트 참조)

## workflow-master 분류

- 복잡도: 간단~중간. 단일 내부 기획 문서 1장. 지배 역할 = Marketer.
- 4역할 관점을 풀 병렬 spawn 대신 **표 6컬럼에 직접 매핑**(marketing-06 검증 선례 계승, 단일 파일 동시 편집 충돌 회피):
  - Planner → `첫 10명 후보 조건` / `Job`
  - Marketer → `첫 문장 / 약속`
  - Developer → `첫 세션 가치 순간` (기존 이벤트/화면 근거)
  - Operator → `승인 필요 외부 액션 경계`
- 작성(marketer 서브에이전트 레인)과 검증(workflow-master 레인)을 분리. 로그: `~/.claude/logs/workflow_2026-05-20_marketing-07.log`.

## actions_taken

- (L0) 근거 문서 4종 정독: `copy-spec.md`, `first-session-jtbd-matrix.md`, source note(2026-05-20-minimum-viable-audience.md), Infinity governance(PERMISSIONS/heartbeat/ARTIFACT_RULES).
- (L0) 양 레포 git 상태 확인: virtue-rebirth-app(master)·prompt-archive(main) 모두 clean, origin과 동기.
- (L1) `marketer` 서브에이전트에 4역할 렌즈 명시 위임 → `docs/minimum-viable-audience-brief.md` 신규 작성.
- (L1) 검증 게이트 실행(아래 verification).
- (L1) 본 report + archive 원장 작성, INTENTS.md Inbox→Archive 이동.
- (L2, agent-approved) virtue-rebirth-app + prompt-archive normal push.

## findings

- source note의 4개 오디언스 후보(감사·사진일기 이탈자 / 라이프 RPG 취향 / AI 코멘트 호기심 / 개인 로그 수집형)가 기존 JTBD 4잡(J1/J2/J3/J4)에 자연 매핑됨 → 새 세그먼트를 만들지 않고 계승만으로 MVA 기준표가 성립.
- "첫 문장/약속"은 수동 초대 시 쓸 후보 카피 초안일 뿐 앱 미반영. copy-spec 금지어 0건으로 작성 가능했음.
- 외부 접촉(DM/메일/공개 포스트/설문/초대 실행)은 전부 Waiting 경계 안. 본 Intent는 "문안 작성까지 L1, 보내는 순간부터 Waiting" 경계를 문서에 못박는 선에서 완결.

## verification

- 필수 리터럴 14/14 존재(rg): `J1 기록형`/`J2 누적형`/`J3 AI 호기심형`/`J4 회고형`, `deed_saved`/`deed_judged`/`level_up_viewed`, 6컬럼명 전부, `Out of scope`, `do things that don't scale`.
- 6컬럼 헤더 1행 확인: `Job (Planner) | 첫 10명 후보 조건 (Planner) | 첫 문장 / 약속 (Marketer) | 첫 세션 가치 순간 (Developer) | 관찰 질문 (Marketer/Planner) | 승인 필요 외부 액션 경계 (Operator)`.
- 활성화 이벤트 매핑(J1/J2/J4=`deed_saved`, J3=`deed_judged`, J2 부가=`level_up_viewed`)이 `first-session-jtbd-matrix.md` §2~§3과 일치 → 충돌 없음.
- copy-spec 금지어는 §4 no-conflict 메타맥락 1곳(line 68)만 등장, 후보 카피 4개에는 0건.
- `git diff --check` 공백 오류 0. virtue-rebirth-app 신규 파일 `docs/minimum-viable-audience-brief.md` 1개뿐, 코드/트래킹/배포 0건.
- unrelated dirty 파일 없음(작업 전 양 레포 clean). 건드린 것 없음.

## L2 자체 승인 체크리스트 (normal push)

- 목표 Intent 직결: 예 (marketing-07 산출물 + bookkeeping).
- 되돌림/재시도 가능: 예 (일반 커밋, force 아님).
- 비용 발생: 없음.
- 프로덕션/시크릿/권한 변경: 없음.
- 타인에게 메시지/메일/알림: 없음 (사용자 본인 보고만).
- 실행 전 상태 확인 + 실행 후 검증 경로: 있음 (push 후 local==origin 확인).

## commits

- repo: virtue-rebirth-app — `docs/minimum-viable-audience-brief.md` 추가. (sha는 caller 최종 응답에 기록)
- repo: prompt-archive — report + archive 원장 + INTENTS.md Inbox→Archive 단일 커밋. (sha는 caller 최종 응답에 기록)

## next_actions

- 수동 첫 세션 관찰 스크립트 초안(15분 흐름 + 질문 5개) → 별도 Intent (외부 발송 없음, L1).
- 외부 초대 승인 요청 템플릿(실발송 없이 승인 조건·리스크만) → 별도 Intent (Waiting 후보).
- 실 사용자 신호 확보 후 후보 조건 → 실제 세그먼트 확정 → 별도 Intent (트래킹 승인 필요).
